[tool.poetry]
name = "to-do-back"
version = "0.1.0"
description = "API de gerenciamento de tarefas"
authors = ["Guilherme Magain Brum <guimbrum@gmail.com>"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.115.0"
uvicorn = "^0.30.0"
sqlalchemy = "^2.0.30"
python-dotenv = "^1.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"